
module.exports =

    # :: (date, date) -> entries (anemic)
    # {userId, entries}
    load: (start, stop) ->

    # :: entry -> entry
    save: (entry) ->

    # :: (date, date) -> [client]
    clients: ->

    # :: (date, date) -> [project]
    projects: ->

    # :: (project) -> (project)
    saveproject: (project) ->

    # :: (client) -> (client)
    saveclient:  (client) ->
